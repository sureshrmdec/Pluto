package com.jiaxin.cc.SortingAndSearching;

/**
 * We will divide the file into chunks which are x megabytes each, where x is the amount of memory we have availanle. 
 * Each chunk is sorted separately and then saved back to the file system.
 * 
 * Once all the chunks are sorted, we then merge the chunks, one by one. At the end, we have a fully sorted file.
 * 
 * @author jeffwan
 * @date May 18, 2014
 */
public class ExternalSort {

}
